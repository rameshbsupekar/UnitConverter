namespace UnitConverter.Common.Constants;

/// <summary>
/// Non-versioned endpoint paths (health, diagnostics).
/// </summary>
public static class EndpointPaths
{
    public const string Health = "/health";
}
